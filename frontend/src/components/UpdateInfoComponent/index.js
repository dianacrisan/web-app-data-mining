import React from 'react';
// import { TopLine, InfoContainer, InfoWrapper, InfoRow, Heading, TextWrapper, Description } from './UpdateInfoComponentElements';

const UpdateInfoComponent = ({ lightBg, id, topLine, lightText, darkText, headline, description, description2, description3, description4 }) => {
    return (
        <>
            {/* update profile section */}
        </>
    )
}

export default UpdateInfoComponent;
